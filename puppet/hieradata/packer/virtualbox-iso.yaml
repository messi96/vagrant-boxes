---
classes:
  - sudo
  - sudo::configs
  - vagrant_base_box
sudo::purge: false
sudo::config_file_replace: false
sudo::configs:
  'vagrant':
    'content'   : "vagrant ALL=(ALL) NOPASSWD: ALL"
    'priority'  : 10
