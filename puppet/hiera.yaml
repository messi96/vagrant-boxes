---
:backends:
  - yaml
:yaml:
  :datadir: /tmp/hieradata
:hierarchy:
  - "packer/%{::packer}"
  - "node/%{::fqdn}"
  - "osfamily/%{::osfamily}"
  - "osfamily/%{::osfamily}-%{::virtual}"
  - "role/%{::role}"
  - "virtual/%{::virtual}"
  - common
