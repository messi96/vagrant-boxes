---
:backends:
  - yaml
:yaml:
  :datadir: /tmp/hieradata
:hierarchy:
  - "packer/%{::packer}"
  - "node/%{::fqdn}"
  - "osfamily/%{::osfamily}"
  - "role/%{::role}"
  - "virtual/%{::virtual}"
  - common
